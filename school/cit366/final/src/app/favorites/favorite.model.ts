export class Favorite {
    public id: number;
    public color: string;
    public word: string;

    constructor(id: number, color: string, word: string) {
        this.id = id;
        this.color = color;
        this.word = word;
    }
}