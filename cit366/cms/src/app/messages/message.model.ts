export class Message {
    constructor(id: Number, subject: String, msgText: String, sender: String) {
        id = id;
        subject = subject;
        msgText = msgText;
        sender = sender;
    }
}